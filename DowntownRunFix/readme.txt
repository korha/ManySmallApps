Choose speed in Downtown Run game

Put advapi33.dll to app folder.
Patch "ADVAPI32.dll" to "ADVAPI33.dll" in Main.pro (use hex-editor).

Fix contains:
- '1' key - restore normal speed game;
- '2' key - 2x slower game speed;
- '4' key - 4x slower game speed.
